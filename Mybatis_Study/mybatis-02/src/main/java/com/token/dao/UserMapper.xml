<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.token.dao.UserDao">


    <!-- 查询语句 -->
    <select id="getUserList" resultType="User" >
        select * from user
    </select>

    <select id="getUserById" parameterType="int" resultType="com.token.pojo.User" >
        select * from user where id = #{id}
    </select>

    <select id="getUserById2" parameterType="map" resultType="com.token.pojo.User" >
        select * from mybatis.user where id = #{id} and name = #{name}
    </select>

    <insert id="addUser" parameterType="com.token.pojo.User">
        insert into user (id, name, pwd) values (#{id},#{name},#{pwd});
    </insert>

    <insert id="addUser2" parameterType="map">
        insert into user (id, pwd) values (#{userid},#{password});
    </insert>


    <update id="updateUser" parameterType="com.token.pojo.User">
        update mybatis.user set name= #{name} , pwd= #{pwd} where id = #{id};
    </update>

    <delete id="deleteUser" parameterType="com.token.pojo.User">
        delete from mybatis.user where id = #{id}
    </delete>

</mapper>


